#include <stdio.h>

/*
 * Skeleton only. Real CLI ships in later TDD steps.
 * Exit 3 is reserved for "not implemented" so it cannot accidentally satisfy
 * design exit codes (0 success, 1 usage/error, 2 not found).
 */
int main(int argc, char **argv)
{
    (void)argc;
    (void)argv;
    fprintf(stderr, "remember: not implemented\n");
    return 3;
}
